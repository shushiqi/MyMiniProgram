function request(url, params, method = "get", options) {
	return new Promise((resolve, reject) => {
		// uni.request({
		// 	url, //仅为示例，并非真实接口地址。
		// 	data: params,
		// 	header: {
		// 		'custom-header': 'hello' //自定义请求头信息
		// 	},
		// 	success: (res) => {
		// 		resolve(res)
		// 	},
		// 	fail:(err)=>{
		// 		reject(err)
		// 	}
		// })
		
	})
}
export default request
